---
interface Props { titre?: string; texte?: string; }
const {
  titre = 'Trois façons de commencer',
  texte = "Une estimation, un guide de prix, ou simplement une conversation. Sans engagement, et sans relance commerciale insistante.",
} = Astro.props;
---
<section class="section bg-[color:var(--color-papier)]" aria-labelledby="cta-final">
  <div class="conteneur">
    <div class="max-w-2xl">
      <h2 id="cta-final" class="titre-2">{titre}</h2>
      <p class="chapo mt-6">{texte}</p>
    </div>
    <div class="mt-10 flex flex-col gap-4 sm:flex-row sm:flex-wrap">
      <a href="/estimation" class="bouton">Estimer mon bien</a>
      <a href="/guides" class="bouton bouton-secondaire">Recevoir le guide des prix</a>
      <a href="/contact" class="bouton bouton-secondaire">Prendre rendez-vous</a>
    </div>
  </div>
</section>
